node_modules
.DS_Store
.env

# Compiled build artifacts — regenerated by "npm run build"
target/babel/server/
target/babel/target/
target/babel/kibana.json
target/babel/package.json

# Keep the distributable zip and the top-level static bundle
!target/babel-*.zip
